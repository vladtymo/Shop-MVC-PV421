@model List<Order>

<h1>Order List</h1>

<ul class="list-group">
    @foreach(var i in Model)
    {
        <li class="list-group-item">#@i.Id - @i.TotalPrice$ made by @i.User?.UserName at @i.Date.ToString()</li>
    }
</ul>